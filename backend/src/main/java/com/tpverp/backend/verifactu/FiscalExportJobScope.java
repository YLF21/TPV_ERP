package com.tpverp.backend.verifactu;

/** Stable scope label shown to clients so a reloaded job remains self-describing. */
public enum FiscalExportJobScope {
    PERIOD,
    FILTERED,
    SELECTED,
    CURRENT
}
