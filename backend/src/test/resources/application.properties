# Integration tests create several Spring contexts with isolated PostgreSQL schemas.
# Keep each Hikari pool deliberately small so cached contexts cannot exhaust the
# database connection limit during the complete test suite.
spring.datasource.hikari.maximum-pool-size=2
spring.datasource.hikari.minimum-idle=0
