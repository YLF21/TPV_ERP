services:
  postgres:
    ports:
      - "127.0.0.1:5433:5432"
  saas-backend:
    environment:
      SPRING_PROFILES_ACTIVE: dev
      TPV_SAAS_CORS_ALLOWED_ORIGINS: ${TPV_SAAS_CORS_ALLOWED_ORIGINS:-http://127.0.0.1:8088,http://localhost:8088}
