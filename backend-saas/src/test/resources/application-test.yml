spring:
  config:
    activate:
      on-profile: test
  datasource:
    url: jdbc:tc:postgresql:17-alpine:///tpv_saas
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    username: test
    password: test
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

tpv:
  saas:
    legacy-basic-auth-enabled: true
    secrets:
      encryption-key: AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA=

